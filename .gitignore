# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp

# Node (not used — no build step — but kept so stray installs never land in the repo)
node_modules/
npm-debug.log*
package-lock.json

# Deploy tooling
.vercel
.netlify

# Local scratch / preview config
.claude/
scratch/
tmp/
*.local
*.log
